package network;

public class Parameters {
	public static final int REFRESH_RATE = 1000; //milliseconds between each redraw 
	public static final int MAX_BUFFER_SIZE = 65000; //maximum bytes per datagrampacket
	public static final int CHANGE_INTERVAL = 3000; //milliseconds time in each TrafficLightState
	public static final int DEFAULT_PORT = 9876; //this is the port used for the server socket, by default. Not clients ports.

}
